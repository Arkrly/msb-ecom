export interface ProductRequest {
  name: string;
  description: string;
  price: number;
}

export interface ProductResponse {
  id: string;
  name: string;
  description: string;
  price: number;
}
